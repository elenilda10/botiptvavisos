export async function handleScheduled(env, controller) {
  console.log(
    "[CRON] Execução agendada recebida:",
    new Date().toISOString()
  );

  // Depois vamos ligar aqui os banners automáticos.
}
